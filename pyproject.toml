[project]
name = "doco-backend-workspace"
version = "0.1.0"
requires-python = ">=3.12,<3.13"

[tool.uv]
package = false
required-version = "==0.11.32"

[tool.uv.workspace]
members = ["packages/contracts", "services/chat"]
